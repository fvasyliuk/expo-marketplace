import { createAsyncActions, createAction} from '@letapp/redux-actions';

export const addKeyword = createAction('persist/ADD_KEYWORD');
export const addLocation = createAction('persist/ADD_LOCATION');