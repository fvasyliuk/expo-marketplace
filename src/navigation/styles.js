import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    labelCenter: {
        textAlign: 'center',
    },
    createNewItem: {
        position: 'absolute',
        bottom: 70,
        left: 160,
        zIndex: 300,
    }
})

export default styles;